
import React from 'react';
import LoginFormModule from './LoginForm/index';

// This component is a simple wrapper for backward compatibility
const LoginForm: React.FC = () => {
  return <LoginFormModule />;
};

export default LoginForm;
